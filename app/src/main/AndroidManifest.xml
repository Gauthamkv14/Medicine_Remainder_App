<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.medicremainder1">

    <!-- Required for alarms and boot events -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MedicRemainder1">

        <!-- MAIN LAUNCH ACTIVITY -->
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

        <!-- OTHER ACTIVITIES -->
        <activity
            android:name=".AddMedicineActivity"
            android:parentActivityName=".MainActivity"
            android:exported="false"/>

        <activity
            android:name=".EditMedicineActivity"
            android:parentActivityName=".MainActivity"
            android:exported="false"/>

        <activity
            android:name=".TimetableActivity"
            android:exported="false"/>

        <activity
            android:name=".ConfirmationActivity"
            android:exported="false"/>

        <!-- RECEIVERS -->

        <!-- Fired exactly at medicine time -->
        <receiver
            android:name=".AlarmReceiver"
            android:exported="true"/>

        <!-- Fired 30 minutes later to mark MISSED if still PENDING -->
        <receiver
            android:name=".MissReceiver"
            android:exported="false"/>

        <!-- Optional snooze receiver -->
        <receiver
            android:name=".SnoozeReceiver"
            android:exported="true"/>

        <!-- Re-registers all alarms after device reboot -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>

        </receiver>

    </application>

</manifest>
